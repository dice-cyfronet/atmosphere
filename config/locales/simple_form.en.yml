en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
    error_notification:
      default_message: "Please review the problems below:"
    labels:
      compute_site:
        site_id: Site ID
        name: Name
        active: Active
        location: Location
        site_type: Site type
        technology: Technology
        http_proxy_url: HTTP Proxy URL
        https_proxy_url: HTTPS Proxy URL
        config: Configuration
        template_filters: Template filters
        wrangler_url: IP Wrangler URL
        wrangler_username: IP Wrangler Username
        wrangler_password: IP Wrangler Password
      security_policy:
        name: Name
        payload: Payload
        users: Users
      security_proxy:
        name: Name
        payload: Payload
        users: Users
      user_key:
        name: Name
        public_key: Public key
        user: User
      virtual_machine_template:
        site: Site
        id_at_site: ID at site
        name: Name
        state: State
    hints:
      defaults:
    placeholders:
      defaults:
    prompts:
      defaults:

  enumerize:
    transport_protocol:
      tcp: TCP
      udp: UDP
    application_protocol:
      http: HTTP
      https: HTTPS
      http_https: HTTP or HTTPS
      none: None
    endpoint_type:
      ws: Web Service
      rest: REST Service
      webapp: Web Application
