%h1 Listing User Keys

%p
  = link_to new_admin_user_key_path,
    class: 'btn btn-success btn-xs', title: 'New User key' do
    =fa_icon 'plus'
    Add new

%p
  %table.table.table-hover.table-condensed
    %tr
      %th Name
      %th Fingerprint
      %th Owner
      %th Actions

    - @user_keys.each do |user_key|
      %tr
        %td= user_key.name
        %td= user_key.fingerprint
        %td= user_key.user.login
        %td
          .btn-group
            = link_to admin_user_key_path(user_key),
              class: 'btn btn-success btn-xs', title: 'Show this User Key' do
              =fa_icon 'bars'
            = link_to admin_user_key_path(user_key), :method => :delete, :data => { :confirm => 'Are you sure?' },
              class: 'btn btn-danger btn-xs', title: 'Remove this User Key' do
              =fa_icon 'trash-o'
