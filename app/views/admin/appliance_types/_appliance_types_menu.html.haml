.col-md-3
  %legend
    Appliance Types
    .pull-right
      = link_to new_admin_appliance_type_path, class: 'btn btn-success btn-xs', title: 'Add new Appliance Type' do
        =fa_icon 'plus'
        Add new

  %ul#appliance-types-menu.nav.nav-pills.nav-stacked
    - @appliance_types.each do |appliance_type|
      %li
        = link_to admin_appliance_type_path(appliance_type) do
          = appliance_type.name
          %small.text-muted
            by
            = appliance_type.author_name
