%h1 Security proxies

%p
  = link_to new_admin_security_proxy_path,
    class: 'btn btn-success btn-xs', title: 'New Security Proxy' do
    =fa_icon 'plus'
    Add new

%p
  %table.table.table-hover.table-condensed
    %tr
      %th Name
      %th Actions

    - @security_proxies.each do |security_proxy|
      %tr
        %td= security_proxy.name
        %td
          .btn-group
            = link_to edit_admin_security_proxy_path(security_proxy),
              class: 'btn btn-warning btn-xs', title: 'Edit this Security Proxy' do
              =fa_icon 'edit'
            = link_to [:admin, security_proxy], :method => :delete, :data => { :confirm => 'Are you sure?' },
              class: 'btn btn-danger btn-xs', title: 'Remove this Security Proxy' do
              =fa_icon 'trash-o'
