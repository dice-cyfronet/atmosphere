%h1
  = t('editing')
  = t('compute_sites.long_title_single')

= render 'form'

%p
  .btn-group
    = link_to admin_compute_site_path(@compute_site),
      class: 'btn btn-success', title: t('compute_sites.show') do
      =icon 'bars'
      = t('show')
    = link_to admin_compute_sites_path,
      class: 'btn btn-default', title: t('back') do
      =icon 'reply'
      = t('back')
