.row
  .col-md-2
    %h2
      = link_to help_path, class: 'btn btn-default btn-xs' do
        =icon 'angle-left'
        = t 'help.back'
      %span.pull-right API
    %hr

    #ul.nav.nav-pills.nav-stacked
      - %w(README compute_sites appliance_types port_mapping_templates endpoints port_mapping_properties appliance_configuration_templates appliance_sets appliances appliance_configuration_instances http_mappings virtual_machines virtual_machine_templates port_mappings dev_mode_property_sets user_keys security_proxies security_policies users appliance_endpoints virtual_machine_flavors).sort.each do |file|
        = nav_tab :category, file do
          = link_to file.titleize, help_api_file_path(file)
  .col-md-10
    = preserve do
      = markdown File.read(@doc_file)
